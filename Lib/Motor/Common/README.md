# Motor Common

本目录用于放置所有 profile 都可以复用的轻量基础组件。

建议放入：

- 数学工具、角度归一化、限幅、单位换算
- PID、低通滤波、斜率限制
- Clarke/Park、反 Park、SVPWM 基础计算
- 与具体观测器、启动方式、BSP 无关的公共类型

约束：

- 不直接访问 BSP/HAL。
- 不持有全局电机状态。
- 不依赖具体 profile。
- 可在 ISR/控制环内调用的函数需要保持无阻塞、无动态分配。
